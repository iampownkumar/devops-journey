services:
  redis:
    image: redis:alpine
    networks:
      - appnet

  api:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - redis
    networks:
      - appnet

networks:
  appnet:
    driver: bridge
